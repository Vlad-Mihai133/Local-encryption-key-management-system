# Postgres
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/klm

# Master key for encrypting key-material before storing it in DB.
# Next sprint: decide format (raw bytes/base64), rotation strategy, etc.
KLM_MASTER_KEY_BASE64=
